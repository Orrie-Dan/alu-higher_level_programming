#!/usr/bin/python3
"""
This module defines a class `MyList` which is a subclass of the built-in
`list` class. The `MyList` class adds an additional method called 
`print_sorted()` that prints the list in ascending order without modifying
the original list.

The class assumes that all elements in the list are integers.

Usage:
    - Create an instance of `MyList`.
    - Use the `print_sorted()` method to print the sorted version of the list.

Example:
    my_list = MyList([3, 1, 2, 4])
    my_list.print_sorted()  # Output: [1, 2, 3, 4]
"""

class MyList(list):
    """
    A subclass of list that adds a method to print the list in sorted order.
    """

    def print_sorted(self):
        """
        Prints the list in ascending order without modifying the original list.
        """
        print(sorted(self))

